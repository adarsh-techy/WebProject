<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: Arial, sans-serif; margin: 0; padding: 20px; }
    .ticket { border: 2px solid #e50914; border-radius: 8px; padding: 20px; max-width: 600px; margin: 0 auto; }
    .header { color: #e50914; text-align: center; margin-bottom: 20px; }
    .event-title { font-size: 24px; font-weight: bold; margin-bottom: 10px; }
    .details { margin-bottom: 15px; }
    .qr-code { text-align: center; margin: 20px 0; }
    .footer { margin-top: 20px; font-size: 12px; color: #666; text-align: center; }
  </style>
</head>
<body>
  <div class="ticket">
    <div class="header">
      <h1>Event Ticket</h1>
    </div>
    <div class="event-title"><%= concert.title %></div>
    <div class="details">
      <p><strong>Booking Reference:</strong> <%= booking._id %></p>
      <p><strong>Date:</strong> <%= concert.date.toLocaleDateString() %></p>
      <p><strong>Time:</strong> 4:00 PM - 11:00 PM</p>
      <p><strong>Venue:</strong> <%= concert.venue %></p>
      <p><strong>Quantity:</strong> <%= booking.quantity %></p>
      <p><strong>Total Price:</strong> $<%= booking.totalPrice %></p>
    </div>
    <div class="footer">
      <p>Present this ticket at the venue for entry</p>
      <p>© <%= new Date().getFullYear() %> EventHub. All rights reserved.</p>
    </div>
  </div>
</body>
</html>